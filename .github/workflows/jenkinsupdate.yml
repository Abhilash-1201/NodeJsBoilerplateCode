name: trigger jenkins job
on: [push]
jobs:

  build:
    name: Build
    runs-on: self-hosted
    steps:
    - name: trigger single Job
      uses: actions/checkout@v3
      with:
        url: "http://3.22.98.199:8080/"
        user: "admin"
        token: ${{ secrets.TOKEN }}
        job: "nodejs"
          
          
