name: trigger jenkins job
on: [push]
jobs:

  build:
    name: Build
    runs-on: self-hosted
    steps:
    - name: trigger single Job
      uses: rlabhilash1201@gmail.com/NodeJsBoilerplateCode
      with:
        url: "http://3.22.98.199:8080/"
        user: "admin"
        token: ${{ secrets.TOKEN }}
        job: "NodeJs_Project"
          
