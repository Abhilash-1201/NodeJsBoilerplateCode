name: trigger jenkins job
on: [push]
jobs:

  build:
    name: Build
    runs-on: self-hosted
    steps:
    - name: trigger single Job
      uses: actions/checkout@v2
      with:
        repository: Abhilash-1201/NodeJsBoilerplateCode
        ref: main
        #url: "http://3.22.98.199:8080/"
        #user: "admin"
        token: ${{ secrets.TOKEN }}
        job: "NodeJs_Project"
          
